export const metadata = {
  title: "Book a Free Consultation | ARK Accounting",
  description: "...",
};

export default function ConsultationLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}