export const metadata = {
  title: "Auditing Services | ARK Accounting",
  description: "Professional audit services including annual financial statement audits, internal control evaluation, internal audit, and special audits — compliant with Egyptian Auditing Standards.",
};

export default function AuditLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}